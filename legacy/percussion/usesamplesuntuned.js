inlets = 1;outlets = 1;autowatch = 1;var lab = "label0";g = new Global("theGlobal");// sets desired label for next samplefunction label(la){	//post("label received: "+la);        	lab = la;}function triggerhit(){     	var choiceArray = [];	i = 0;	//post("g.labelArray.length = " + g.labelArray.length + "\n");       	while(i < g.labelArray.length)	{		//post("g.labelArray[i] = "+ g.labelArray[i]+"\n");               		if(lab == g.labelArray[i])		{			choiceArray.push(i);		}		i++;	}	//post("lab="+lab+"\n");	// choiceArray contains the indexes into both labelArray and fileArrayUn.        	//post("choiceArray length = "+choiceArray.length+"\n");	//post("randi = "+randi+"\n");		randi = Math.floor(Math.random() * choiceArray.length);	chosenIndex = choiceArray[randi];	outlet(0, chosenIndex);	//post("chosenIndex = "+chosenIndex+"\n");}